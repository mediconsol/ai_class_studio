[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = [
  "npm ci",
  "npm run prisma:generate"
]

[phases.build]
cmds = [
  "npm run build"
]

[start]
cmd = "npm run prisma:migrate && npm start"
