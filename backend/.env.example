# ============================================
# 백엔드 환경변수 (Railway 배포)
# ============================================

# Database URL (Railway가 자동 생성, 로컬은 수동 설정)
# 로컬: postgresql://user:password@localhost:5432/aiclass
# Railway: 자동 생성됨
DATABASE_URL="postgresql://user:password@localhost:5432/aiclass"

# JWT Secret (강력한 랜덤 문자열)
# 생성 방법: openssl rand -base64 32
JWT_SECRET="your-super-secret-jwt-key-here-change-this"

# Node Environment
NODE_ENV="development"

# Server Port (Railway는 자동, 로컬은 3000)
PORT=3000

# CORS Origin (프론트엔드 URL)
# 로컬: http://localhost:7900
# 프로덕션: https://inno.mediconsol.com,https://aiclassstudio.vercel.app
CORS_ORIGIN="http://localhost:7900"
