# ============================================
# Railway 프로덕션 환경변수 (Raw Editor용)
# ============================================
# 아래 내용을 복사하여 Railway 백엔드 서비스의
# Variables → Raw Editor에 붙여넣기
# ============================================

# PostgreSQL 데이터베이스 연결
DATABASE_URL=${{Postgres.DATABASE_URL}}

# JWT 인증 시크릿 (아래 명령어로 생성)
# node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=CHANGE_THIS_TO_RANDOM_SECRET_KEY

# Node 환경
NODE_ENV=production

# 서버 포트
PORT=3000

# CORS 허용 도메인 (쉼표로 구분, 공백 없음)
CORS_ORIGIN=https://inno.mediconsol.com,https://aiclassstudio.vercel.app

# ============================================
# AI API Keys (보안 - 백엔드에서만 사용)
# ============================================

# Claude (Anthropic)
ANTHROPIC_API_KEY=sk-ant-api03-YOUR_ANTHROPIC_KEY_HERE

# OpenAI (GPT)
OPENAI_API_KEY=sk-YOUR_OPENAI_KEY_HERE

# Google AI (Gemini)
GOOGLE_API_KEY=YOUR_GOOGLE_API_KEY_HERE

# ============================================
# 선택적 설정
# ============================================

# AI 요청 타임아웃 (밀리초)
AI_REQUEST_TIMEOUT=30000

# AI 요청 최대 재시도 횟수
AI_MAX_RETRIES=3

# 로그 레벨 (error | warn | info | debug)
LOG_LEVEL=info
